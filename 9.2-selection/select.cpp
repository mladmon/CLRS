#include <iostream>
#include <string>
#include <exception>
#include <chrono>
#include <random>
#include <unordered_set>
#include <vector>

using namespace std;

void partition(unordered_set<int> &s, int p, int r) {

}

int random(int p, int r) {

}

void randomizedPartition(unordered_set<int> &s, int p, int r) {

}

/* Return the ith order statistic from a set of n distinct numbers (i.e., the
   ith smallest number) */
int randomizedSelect(unordered_set<int> &s, int p, int r, int i) {

}

int main() {
	// Generate a set S[0..n-1] of n distinct numbers
	

	// Ask the user for an integer i in the range [1-n]

	// Call randomizedSelect() to find the ith smallest number in S[0..n-1]

	return 0;
}
