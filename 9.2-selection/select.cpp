#include <iostream>
#include <string>
#include <exception>
#include <chrono>
#include <random>
#include <vector>

using namespace std;

void partition(vector<int> &v, int p, int r) {

}

int random(int p, int r) {
	return 0;
}

void randomizedPartition(vector<int> &v, int p, int r) {

}

/* Return the ith order statistic from a set of n distinct numbers (i.e., the
   ith smallest number) */
int randomizedSelect(vector<int> &v, int p, int r, int i) {
	return 0;
}

int main() {
	// Generate a set A[0..n-1] of n distinct numbers

	// Ask the user for an integer i in the range [1-n]

	// Call randomizedSelect() to find the ith smallest number in A[0..n-1]

	return 0;
}
